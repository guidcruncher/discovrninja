<div id="news" class="newsprint">
  <div class="content">
    <table border="0" cellpadding="2" cellspacing="0">
      <tr class="masthead">
        <td>
          <a href="#" onclick="newsPage('{{feedUrl}}'); return false;"><img src="{{meta.image.url}}" /></a>
        </td>
      </tr>
      <tr>
        <td style="border-bottom: solid 1px black">
          {{meta.pubDateStr}}
        </td>
      </tr>
      {{#each items}}
        <tr>
          <td>
            <p class="headline"><a href="{{link}}" target="_new" style="text-decoration: none !important;">{{{title}}}</a></p>
            {{#if image}}<img src="{{image.url}}" />{{/if}}
            <p>{{{description}}}</p>
          </td>
        </tr>
      {{/each}}
    </table>
  </div>
</div>
